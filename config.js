var info = "The password must contain the following:"

var prerequisites = {
    // the item name needs to be the same as the id listed
    // within the html prerequisites list
    lowerCase: "At least one lower case letter [a-z]",
    upperCase: "At least one upper case letter [A-Z]",
    number: "At least one number [0-9]",
    symbol: "At least one symbol [!@#^&*()+_,.{}[]?-]",
    minLength: "Minimum 6 characters",
};
